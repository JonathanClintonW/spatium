{
  "name": "spatium",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run develop --prefix cms\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
